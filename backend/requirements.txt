# Pinned to a combination verified to install and pass the test suite together.
# Note: cirq-core 1.4.x requires numpy<2 while newer PennyLane requires numpy>=2,
# so numpy is held below 2.0 and PennyLane/pennylane-qiskit are pinned to match.

# --- API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
orjson==3.10.12
email-validator==2.2.0

# --- database
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
alembic==1.14.0

# --- validation / settings
pydantic==2.10.3
pydantic-settings==2.6.1

# --- auth
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt >= 4.1
PyJWT==2.10.1

# --- async workers
celery==5.4.0
redis==5.2.1

# --- quantum stack
numpy==1.26.4
qiskit==1.2.4
qiskit-aer==0.16.0
openqasm3[parser]==1.0.0
cirq-core==1.4.1
pennylane==0.44.1
pennylane-qiskit==0.42.0

# --- AI + remote execution
google-generativeai==0.8.3
qbraid==0.12.2  # 0.8.x is incompatible with current pydantic

# --- misc
httpx==0.28.1
pytest==8.3.4
